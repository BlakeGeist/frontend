'use strict';

const Joi = require('joi');
const buildSchema = require('./../build-schema');
const common = require('../common');

const baseFields = {
  id: common.id,
  name: common.name,
  description: common.description,
  user_id: common.id,
  click_count: Joi.number().integer().min(0),
  social_click_count: Joi.number().integer().min(0),
  hash: Joi.string().max(40),
  params: Joi.string(),
  active: common.active
};

const requiredForPost = ['user_id'];

const attributeFields = {};

const additionalForbiddenAttribs = [];

module.exports = buildSchema(baseFields, requiredForPost, attributeFields, additionalForbiddenAttribs);
