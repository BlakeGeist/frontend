(function(ctx) {
  'use strict;'
  ctx.data = {{stringify data}};
  ctx.pageSettings = {{stringify pageSettings}};
  ctx.fireBaseData = {{stringify fireBaseData}};
  ctx.strings = {{stringify strings}};
  ctx.products = {{stringify products}};
  ctx.globalSiteSettings = {{stringify globalSiteSettings}};
  ctx.siteSettings = {{stringify siteSettings}};
  ctx.posts = {{stringify posts}};

})(window.site.context);
